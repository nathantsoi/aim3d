[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*

# Coverage is opt-in: `make test-python-coverage` (or `pytest --cov=aim3d`)
# enables it. We do not put --cov in addopts so the normal test run stays fast
# and doesn't require pytest-cov to be installed.
